# Vendor Directory Reference

## Scope

High-level reference for Composer-managed dependencies under `vendor/`.

## Guidance

- Treat `vendor/` as generated third-party code.
- Do not apply direct edits in feature branches.
- Manage dependency changes through `composer.json` and lockfile updates.
